name: Lint

on:
  pull_request:
    branches: [ main, dev ]
    
jobs:
  eslint:
    name: ES-Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: stefanoeb/eslint-action@1.0.2
